# Infrastructure Branch Note

This branch intentionally does not activate a UE5.8 self-hosted workflow yet. The real project skeleton and trusted runner must exist first. Activating CI before those dependencies are real would create a failing or unsafe workflow and would not constitute progress.
